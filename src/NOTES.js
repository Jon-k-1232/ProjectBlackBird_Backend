// ToDo in createInvoice-Router, need to add in for discounts, and interest calculation
// ToDo in createInvoice-Router, need refactor. <- callback hell. Look at chaining.

// ToDo create end point-  User needs to create a single new invoice for a perticular company.
// ToDo add in Error handleing all Endpoints
// ToDo look for table join refactor oppertunities in all endpoints to reduce queries.
// ToDo in transactions need to account for adjustments senario.
// ToDo in creat invoices- need to get a pdf creator working in order to make each pdf and place in file for download.

// let promise = new Promise(function (resolve, reject) {
// 	setTimeout(() => resolve({ msg: 'To do some more job' }), 1000)
// })

// promise.then(function (result) {
// 	return { data: 'some data' }
// })

// promise.then(function (result) {
// 	return { data: 'some other data' }
// })

// promise.then(function (result) {
// 	return { data: 'some more data' }
// })
